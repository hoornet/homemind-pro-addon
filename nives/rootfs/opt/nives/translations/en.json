{
  "config": {
    "step": {
      "user": {
        "title": "Configure Nives",
        "description": "Enter the URL of your Nives API server. Don't have a server? Get a hosted one at [{cloud_url}]({cloud_url}).",
        "data": {
          "api_url": "API URL",
          "api_token": "API Token (optional)",
          "user_id": "Default User ID (optional)"
        },
        "data_description": {
          "api_token": "Required for cloud-hosted Nives. Leave empty for self-hosted setups without authentication.",
          "user_id": "Used to namespace memories per user. Leave as 'default' unless you have multiple users."
        }
      },
      "hassio_confirm": {
        "title": "Nives Detected",
        "description": "The Nives add-on has been detected. Would you like to set it up?"
      }
    },
    "error": {
      "cannot_connect": "Failed to connect to the Nives API. Make sure it's running and accessible.",
      "invalid_auth": "Invalid API token. Check your token on the Nives dashboard.",
      "unknown": "Unexpected error occurred."
    },
    "abort": {
      "already_configured": "Nives is already configured.",
      "cannot_connect": "Could not connect to the Nives add-on. Make sure it's running."
    }
  },
  "entity": {
    "conversation": {
      "nives": {
        "name": "Nives"
      }
    }
  },
  "issues": {
    "restart_required": {
      "title": "Restart required",
      "fix_flow": {
        "step": {
          "confirm": {
            "title": "Restart required",
            "description": "Home Assistant needs a restart to finish updating Nives ({running_version} → {installed_version}). Click Submit to restart Home Assistant now — or close this and restart later, at a moment that suits you. Nives keeps working in the meantime."
          }
        }
      }
    }
  }
}
