configuration:
  llm_mode:
    name: LLM Mode
    description: >-
      Choose "cloud" to use the managed Nives AI service, or "byok" to bring your own API key.
  cloud:
    name: "Nives Cloud"
    description: >-
      Settings for Nives Cloud mode. Get your API key at nives.house.
    api_key:
      name: Nives API Key
      description: >-
        Your Nives API key from nives.house. Required when LLM Mode is "cloud".
  byok:
    name: "Bring Your Own Key (BYOK)"
    description: >-
      Settings for using your own API key. Best-effort; for serious local/custom-model setups, see home-mind (open source). Only used when LLM Mode is "byok".
    provider:
      name: Provider
      description: >-
        Which LLM provider to use. OpenRouter gives access to many models with a single key.
    api_key:
      name: API Key
      description: >-
        Your API key for the selected provider.
    model:
      name: Model
      description: >-
        Model ID to use. Examples: anthropic/claude-haiku-4.5 (OpenRouter),
        claude-haiku-4-5-20251001 (Anthropic), gpt-4o-mini (OpenAI). Leave empty for provider default.
    base_url:
      name: API Base URL
      description: >-
        Custom API endpoint. Required for Ollama (e.g. http://homeassistant:11434/v1).
        Leave empty for cloud providers.
  custom_prompt:
    name: Custom Prompt
    description: >-
      Give the assistant its personality — whatever you write here replaces the
      default Nives persona outright, so one sentence describing who it is does
      the job (e.g. "You are HAL 9000, the calm and precise computer from 2001").
      Applies once the add-on restarts. Leave empty for the default Nives persona.
  log_level:
    name: Log Level
    description: >-
      Logging verbosity. Use "debug" for troubleshooting.
